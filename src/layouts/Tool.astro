---
interface Props {
  title: string;
  description?: string;
}

const { title, description = "Remoun's corner of the internet" } = Astro.props;
const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <title>{title}</title>
  <link rel="alternate" type="application/rss+xml" title="RSS" href="https://remoun.blog/rss.xml" />
</head>
<body>
  <header class="site-header">
    <div class="inner inner--wide">
      <a href="/" class="title">Remoun</a>
      <nav>
        <a href="https://remoun.blog" class:list={[{ active: currentPath.startsWith('/blog') }]}>Blog</a>
        <a href="https://remoun.me/portfolio" class:list={[{ active: currentPath.startsWith('/portfolio') }]}>Portfolio</a>
        <a href="https://remoun.me/resume" class:list={[{ active: currentPath === '/resume' }]}>Resume</a>
      </nav>
    </div>
  </header>

  <main class="container--wide">
    <slot />
  </main>

  <footer class="site-footer">
    <div class="container--wide">
      &copy; {new Date().getFullYear()} Remoun
    </div>
  </footer>
</body>
</html>

<style is:global>
  @import '../styles/global.css';

  .site-header .inner--wide {
    max-width: 72rem;
  }

  .container--wide {
    max-width: 72rem;
    margin: 0 auto;
    padding: 2rem 1.5rem;
  }
</style>
